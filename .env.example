# ========================================
# App - Credentials (REQUIRED)
# ========================================
APP_URL=your-app-url
APP_USER=your-email
APP_PASS=your-password
# Tenant/farm selection uses identifier (fallback: APP_TENANT/APP_FARM if identifier not set)
APP_TENANT_IDENTIFIER=your-tenant-identifier
APP_FARM_IDENTIFIER=your-farm-identifier
# Legacy / display (optional): APP_TENANT=your-tenant-name  APP_FARM=your-farm-name

# ========================================
# App - Test Location (REQUIRED for /overview)
# ========================================
APP_LOCATION_TYPE=your-location-type
APP_LOCATION_IDENTIFIER=your-location-identifier

# ========================================
# App API (REST) — mirrors webapp REACT_APP_ROOT_API
# @contract oracles: /stats/tags, /stats/room-tags, /farms/current, …
# See docs/e2e/api-and-cube.md
# ========================================
API_BASE_URL=https://api.dev.xiot.com.au/api/202312
# Optional: X-Farm-Identifier for REST/Cube when APP_FARM_IDENTIFIER is a UI label
# APP_API_FARM_IDENTIFIER=GROOVE
# Optional: tenant timezone for Cube inDateRange (default Australia/Perth)
# APP_TIMEZONE=Australia/Perth

# ========================================
# Cube.js — mirrors webapp REACT_APP_CUBE_API
# Token via POST {API_BASE_URL}/cube/token (use appApi / cubeApi fixtures)
# ========================================
CUBE_API_URL=https://cube.dev.xiot.com.au/cubejs-api/v1

# ========================================
# Admin API — @business QC gate (UAT source of truth)
# ADMIN_URL = base only, no trailing slash
# ========================================
ADMIN_URL=your-admin-api-url
ADMIN_USER=your-admin-username
ADMIN_PASS=your-admin-password

# ========================================
# Integrations (OPTIONAL — CI only, use GitHub secret SLACK_WEBHOOK_URL)
# ========================================
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
